== README

#scaffolding
rails g scaffold Host hash:digest:uniq --no-helper --no-assets --template-engine=null



sudo apt-get install postgresql-contrib
sudo -u postgres psql fenix -c 'create extension hstore;'

